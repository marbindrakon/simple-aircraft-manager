# Build-time notice tooling — NOT bundled into the desktop runtime.
#
# Used by scripts/generate_third_party_notices.py to enumerate the runtime
# dependency set and emit THIRD-PARTY-NOTICES.txt plus per-package license
# files. See COMPLIANCE_PLAN.md (Phase 2) for the rationale.
#
# Install with:
#   pip install -r requirements-build.txt
# Run AFTER -r requirements.txt and -r requirements-desktop.txt and BEFORE
# requirements-desktop-build.txt so PyInstaller stays out of runtime notices.
pip-licenses==5.5.5
prettytable==3.17.0
wcwidth==0.6.0
